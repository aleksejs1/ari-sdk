const React = (window as any).React;

export default React;
export const {
    useState,
    useEffect,
    useContext,
    useReducer,
    useCallback,
    useMemo,
    useRef,
    useImperativeHandle,
    useLayoutEffect,
    useDebugValue,
    Fragment,
    Suspense,
    lazy,
    memo,
    createContext,
    createElement,
    cloneElement,
    isValidElement,
    Children,
    forwardRef,
    StrictMode,
    Component,
    PureComponent,
    startTransition,
    useId
} = React;
